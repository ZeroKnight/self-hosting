# vim: ft=systemd

[Unit]
Description=Back up the '%i' restic repository every day at 3am

[Timer]
Unit=resticprofile-%i@backup.service
OnCalendar=03:00
RandomizedDelaySec=30
Persistent=true

[Install]
WantedBy=timers.target
