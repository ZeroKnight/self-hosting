# Common service settings for SteamCMD game servers

[Unit]
Requires=network.target
After=network.target

[Service]
User={{ server_user.name }}
Group={{ server_user.name }}

Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
